# AutoML Pipeline Optimizer

Automated ML pipeline with preprocessing, model selection, and hyperparameter tuning.

## Quick Start

pip install -r requirements.txt
python examples/basic_usage.py

## Features
- Automatic data preprocessing (imputation, encoding, scaling, outliers)
- Multiple ML models (classification + regression)
- Hyperparameter optimization (grid search)
- Comprehensive evaluation metrics
- JSON report generation
